# springboot-demo

用于后续发布流程验证的最小 Spring Boot 示例项目。

## 运行

```bash
cd /home/nan/workspace/springboot-demo
mvn spring-boot:run
```

## 验证

```bash
curl http://127.0.0.1:8080/
curl http://127.0.0.1:8080/health
```
